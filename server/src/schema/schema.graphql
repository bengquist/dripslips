type Item {
  id: ID!
  title: String!
  description: String!
  price: Float!
  size: Float!
  type: String!
  gender: String!
  image: String!
}

type Query {
  helloWorld: String!
  items(type: String): [Item]
  item(id: ID!): Item
}
