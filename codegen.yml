overwrite: true
schema: "http://localhost:4000/graphql"
documents: "server/src/schema/schema.graphql"
generates:
  generated:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
  ./graphql.schema.json:
    plugins:
      - "introspection"
