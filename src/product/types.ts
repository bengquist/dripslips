export type Product = {
  id: string;
  modelId: string;
  title: string;
  description: string;
  price: number;
  type: string;
  colors: string[];
  gender: string;
  availableSizes: number[];
  images: string[];
};
