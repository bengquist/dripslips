query Product($id: String!) {
  product(id: $id) {
    modelId
    title
    description
    price
    gender
    productDetails {
      size
      color
      productImages {
        url
      }
    }
  }
}
